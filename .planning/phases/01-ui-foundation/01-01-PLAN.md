---
phase: 01-ui-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [index.html, app.js]
autonomous: true
---

<objective>
Add FlutterFlow API Key and Project ID input fields to the existing API Keys modal.

Purpose: Enable users to configure FlutterFlow credentials alongside their AI API keys, preparing for the direct commit feature.
Output: Extended API Keys modal with two new credential fields (FF API Key and FF Project ID) with consistent styling and encryption support.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@index.html
@app.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add FlutterFlow API Key input field to modal</name>
  <files>index.html</files>
  <action>Add a new input field for FlutterFlow API Key in the API Keys modal, following the exact same pattern as the existing Gemini/Anthropic/OpenAI fields:

1. Insert a new section AFTER the OpenAI API Key section (before the security info box)
2. Use the same HTML structure:
   - Label with FlutterFlow logo/icon (use a simple SVG or text indicator)
   - Password input with id="flutterflow-api-key-input"
   - Eye icon toggle button for visibility
   - Status indicator div with id="flutterflow-key-status"
3. Match styling: api-key-input class, same spacing (space-y-6 on container)
4. Use FlutterFlow brand colors (typically orange/red tones - use #FF6B35 or similar)
5. Placeholder text: "Enter your FlutterFlow API key"

The field should appear between the OpenAI section and the blue security info box.</action>
  <verify>grep -q 'flutterflow-api-key-input' index.html && grep -q 'flutterflow-key-status' index.html</verify>
  <done>FlutterFlow API Key input field exists in modal with matching styling and structure</done>
</task>

<task type="auto">
  <name>Task 2: Add FlutterFlow Project ID input field to modal</name>
  <files>index.html</files>
  <action>Add a new input field for FlutterFlow Project ID in the API Keys modal:

1. Insert AFTER the FlutterFlow API Key section (before the security info box)
2. Use similar HTML structure but simplified (no eye toggle needed for Project ID as it's not sensitive):
   - Label with text "FlutterFlow Project ID"
   - Text input (not password) with id="flutterflow-project-id-input"
   - Status indicator div with id="flutterflow-project-status"
3. Use the same api-key-input class for consistent styling
4. Placeholder text: "Enter your FlutterFlow Project ID"
5. Add a help text hint below explaining where to find it: "Found in your FlutterFlow project URL or Settings"

The Project ID field should appear after the FF API Key field and before the security info box.</action>
  <verify>grep -q 'flutterflow-project-id-input' index.html && grep -q 'flutterflow-project-status' index.html</verify>
  <done>FlutterFlow Project ID input field exists with appropriate styling and help text</done>
</task>

<task type="auto">
  <name>Task 3: Add status dot indicators for FlutterFlow credentials</name>
  <files>index.html</files>
  <action>Add status indicators for FlutterFlow credentials in the sidebar API Keys button:

1. Locate the api-keys-status div in the sidebar (currently has 3 dots for Gemini, Claude, OpenAI)
2. Add a 4th dot for FlutterFlow BEFORE the closing div
3. Use same structure: div with class "key-status-dot" and title="FlutterFlow"
4. The dots should be in order: Gemini, Claude, OpenAI, FlutterFlow

Update the CSS if needed to accommodate 4 dots in the flex container (gap-1 should work fine).</action>
  <verify>grep -A5 'api-keys-status' index.html | grep -q 'FlutterFlow'</verify>
  <done>Sidebar API Keys button shows 4 status dots including FlutterFlow</done>
</task>

<task type="auto">
  <name>Task 4: Extend app.js encryption and storage for FlutterFlow credentials</name>
  <files>app.js</files>
  <action>Extend the API key management system in app.js to support FlutterFlow credentials:

1. Add new global variables after the existing API key variables (around line 520):
   - let flutterflowApiKey = "";
   - let flutterflowProjectId = "";

2. Extend initializeApiKeys() function to load FlutterFlow credentials:
   - Add: flutterflowApiKey = await getApiKey("flutterflow");
   - Add: flutterflowProjectId = await getApiKey("flutterflow_project_id");

3. Extend the hasStoredKey() function to include flutterflow:
   - Add "flutterflow" to the keys object

4. Create a separate check function or extend hasStoredKey for project ID

5. Extend updateKeyStatus() and updateModalKeyStatuses() to handle the new fields:
   - Add calls for "flutterflow" with status element "flutterflow-key-status"
   - Add calls for project ID with "flutterflow-project-status"

6. Extend loadApiKeyInputs() to populate the FlutterFlow fields:
   - Set placeholder based on whether credentials are stored

7. Extend saveApiKeys() to save FlutterFlow credentials:
   - Get values from flutterflow-api-key-input and flutterflow-project-id-input
   - Save using saveApiKey("flutterflow", ...) and saveApiKey("flutterflow_project_id", ...)

8. Extend clearAllApiKeys() to clear FlutterFlow credentials

9. Extend updateApiKeyStatusIndicators() to show FlutterFlow status in sidebar

Follow the EXACT same patterns used for the existing three API keys.</action>
  <verify>grep -q 'flutterflowApiKey' app.js && grep -q 'flutterflow_project_id' app.js && grep -q 'flutterflow-key-input' app.js</verify>
  <done>app.js fully supports FlutterFlow credentials with encryption, storage, and UI updates</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Open API Keys modal and verify both FlutterFlow fields are visible
- [ ] Verify FlutterFlow API Key field has password toggle (eye icon)
- [ ] Verify Project ID field is text input (not password)
- [ ] Enter test values and click Save - verify no console errors
- [ ] Refresh page and reopen modal - verify saved values are indicated
- [ ] Sidebar shows 4 status dots including FlutterFlow
</verification>

<success_criteria>

- All tasks completed
- Both FlutterFlow credential fields visible in API Keys modal
- Fields follow consistent styling with existing API key inputs
- Encryption and storage works for both credentials
- Status indicators show in sidebar button
- No JavaScript errors in console
  </success_criteria>

<output>
After completion, create `.planning/phases/01-ui-foundation/01-01-SUMMARY.md`
</output>

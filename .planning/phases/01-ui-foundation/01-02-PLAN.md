---
phase: 01-ui-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [index.html, app.js]
autonomous: true
---

<objective>
Add validation logic and help text for FlutterFlow credentials to guide users and prevent errors.

Purpose: Ensure users enter valid FlutterFlow credentials format and understand where to find them, reducing support issues.
Output: Input validation, helpful guidance text, and visual feedback for credential format errors.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@index.html
@app.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add help text and guidance for FlutterFlow credentials</name>
  <files>index.html</files>
  <action>Add contextual help text and links for both FlutterFlow credential fields:

1. For FlutterFlow API Key field:
   - Add a small info paragraph below the status indicator
   - Text: "Get your API key from FlutterFlow → Settings → API Access"
   - Link "Settings → API Access" to https://app.flutterflow.io/settings/api
   - Style: text-xs text-gray-500, hover:text-blue-600 for link

2. For FlutterFlow Project ID field:
   - Enhance the existing help text (if any) or add below status
   - Text: "Found in your project URL: flutterflow.io/project/[PROJECT-ID]"
   - Add example: "e.g., my-app-1234-abcd"
   - Style: text-xs text-gray-500

3. Add a blue info box at the bottom of the modal (before the action buttons) explaining the FlutterFlow integration:
   - Title: "FlutterFlow Direct Commit"
   - Text: "When configured, you can commit generated code directly to your FlutterFlow project from Step 3 (Code Dissector)."
   - Use similar styling to the existing security info box (bg-blue-50, border-blue-200)

All help text should be visually distinct but not overwhelming - use muted colors and small font sizes.</action>
  <verify>grep -q 'Settings → API Access' index.html && grep -q 'flutterflow.io/project' index.html && grep -q 'FlutterFlow Direct Commit' index.html</verify>
  <done>Helpful guidance text and links added for both credential fields with clear instructions</done>
</task>

<task type="auto">
  <name>Task 2: Implement client-side validation for FlutterFlow credentials</name>
  <files>app.js</files>
  <action>Add validation functions for FlutterFlow credentials in app.js:

1. Create validation helper functions:
   ```javascript
   function validateFlutterFlowApiKey(key) {
     // FF API keys are typically long alphanumeric strings
     // Basic validation: non-empty, reasonable length (min 20 chars), no spaces
     if (!key || key.trim().length < 20) return false;
     if (key.includes(' ')) return false;
     return /^[a-zA-Z0-9_-]+$/.test(key);
   }
   
   function validateFlutterFlowProjectId(projectId) {
     // FF Project IDs are alphanumeric with dashes, typically format: name-1234-abcd
     if (!projectId || projectId.trim().length < 5) return false;
     if (projectId.includes(' ')) return false;
     return /^[a-zA-Z0-9-]+$/.test(projectId);
   }
   ```

2. Extend saveApiKeys() to validate before saving:
   - If FlutterFlow API Key is entered but invalid, show alert and prevent save
   - If Project ID is entered but invalid, show alert and prevent save
   - Use confirm() or custom modal for validation errors
   - Message example: "Invalid FlutterFlow API Key format. API keys should be at least 20 characters and contain only letters, numbers, underscores, and dashes."

3. Add real-time validation feedback:
   - Add input event listeners to FlutterFlow fields
   - Show visual indicator (border color change) when input is invalid
   - Green border when valid, red border when invalid, default when empty
   - Use inline validation (don't block typing, just show state)

4. Create helper function to update input validation state:
   ```javascript
   function updateInputValidationState(inputId, isValid) {
     const input = document.getElementById(inputId);
     if (!input.value) {
       input.style.borderColor = ''; // Reset to default
     } else if (isValid) {
       input.style.borderColor = '#22c55e'; // Green
     } else {
       input.style.borderColor = '#ef4444'; // Red
     }
   }
   ```

Validation should be helpful, not annoying - allow users to type but give immediate feedback.</action>
  <verify>grep -q 'validateFlutterFlowApiKey' app.js && grep -q 'validateFlutterFlowProjectId' app.js && grep -q 'updateInputValidationState' app.js</verify>
  <done>Validation functions implemented with real-time feedback and save-time validation</done>
</task>

<task type="auto">
  <name>Task 3: Add inline validation messages below inputs</name>
  <files>index.html</files>
  <action>Add validation message containers below each FlutterFlow input field:

1. For FlutterFlow API Key:
   - Add a div with id="flutterflow-api-key-error" after the status indicator
   - Default: hidden (display: none)
   - Style: text-xs text-red-600 mt-1
   - Message content: "API key should be at least 20 characters"

2. For FlutterFlow Project ID:
   - Add a div with id="flutterflow-project-id-error" after the status indicator  
   - Default: hidden
   - Style: text-xs text-red-600 mt-1
   - Message content: "Project ID should be at least 5 characters with only letters, numbers, and dashes"

3. In app.js, extend the validation to show/hide these messages:
   - When validation fails, show the error message (display: block)
   - When validation passes or field is empty, hide the message (display: none)

4. Add CSS class for validation states if needed:
   ```css
   .validation-error {
     color: #dc2626;
     font-size: 12px;
     margin-top: 4px;
     display: none;
   }
   .validation-error.visible {
     display: block;
   }
   ```

Keep error messages specific and actionable - tell users exactly what's wrong and how to fix it.</action>
  <verify>grep -q 'flutterflow-api-key-error' index.html && grep -q 'flutterflow-project-id-error' index.html</verify>
  <done>Validation message containers added and wired to validation logic</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Help text is visible below both FlutterFlow credential fields
- [ ] "Settings → API Access" link is clickable and opens correct URL
- [ ] Enter invalid API key (less than 20 chars) - border turns red and error message shows
- [ ] Enter valid API key (20+ alphanumeric chars) - border turns green
- [ ] Try to save invalid credentials - validation prevents save with clear error message
- [ ] Save valid credentials - works without errors
</verification>

<success_criteria>

- All tasks completed
- Helpful guidance text visible for both credential fields
- Validation provides immediate visual feedback (border colors)
- Save button validates credentials before encrypting
- Clear error messages explain format requirements
- No JavaScript errors during validation
- User experience is smooth and helpful
  </success_criteria>

<output>
After completion, create `.planning/phases/01-ui-foundation/01-02-SUMMARY.md`
</output>

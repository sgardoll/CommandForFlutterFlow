---
phase: 11-project-list-dropdown
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [app.js, index.html]
autonomous: true
---

<objective>
Implement project list dropdown in API Keys modal.

Purpose: After user enters their FlutterFlow API key, automatically fetch their projects via the `listProjects` endpoint and display them in a dropdown, eliminating manual copy-paste of Project ID. Simplify the modal UI by reducing or removing the two warning boxes to make room for the dropdown.

Output:
1. `listProjects` method added to FlutterFlowApiClient
2. Dropdown select element for projects in API Keys modal
3. Real-time project fetching when API key is entered/validated
4. Reduced warning boxes (smaller or removed)
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Relevant source files:
@index.html (API Keys modal structure)
@app.js (FlutterFlowApiClient, saveApiKeys, validation functions)

**Current API Keys modal structure:**
- FlutterFlow API Key input (id="flutterflow-api-key-input")
- FlutterFlow Project ID input (id="flutterflow-project-id-input")
- Two warning boxes: orange "FlutterFlow Direct Commit" and blue "Security"
- "Save & Close" button already exists (from quick task 008)

**FlutterFlowApiClient current methods:**
- constructor, getters, pullCode, pushCode
- No `listProjects` method yet

**FlutterFlow API documentation:**
- Projects list endpoint likely `/v1/projects` or similar (need to research)
- Authentication via Bearer token (API key)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add listProjects method to FlutterFlowApiClient</name>
  <files>app.js</files>
  <action>
1. Research the FlutterFlow API endpoint for listing projects:
   - Check VS Code extension's FlutterFlowApiClient.ts for existing pattern
   - Likely endpoint: GET `/v1/projects` with Bearer auth
   - Returns array of projects with `id`, `name`, etc.

2. Add `listProjects` async method to FlutterFlowApiClient class:
   - Accepts optional parameters (page, limit)
   - Makes authenticated GET request to `${this.baseUrl}/projects`
   - Handles response parsing and error handling consistent with pullCode/pushCode
   - Returns array of project objects with at least `id` and `name`

3. Ensure method follows existing patterns:
   - Use same error handling (throw on non-ok response)
   - Include proper JSDoc comments
   - Log for debugging

**Implementation notes:**
- Place method after `pushCode` method in class
- Use `this.apiKey` for Authorization header
- No need for projectId or branchName in this request
- Response format: likely `{ projects: [...] }`
  </action>
  <verify>grep -q 'async listProjects' app.js</verify>
  <done>listProjects method exists and returns project array</done>
</task>

<task type="auto">
  <name>Task 2: Modify API Keys modal UI</name>
  <files>index.html</files>
  <action>
1. Add dropdown select element for projects:
   - Insert after FlutterFlow API Key input, before Project ID input
   - HTML structure:
     ```html
     <div id="flutterflow-projects-container" class="hidden">
       <label class="block text-sm font-medium text-gray-700 mb-2">
         <span class="flex items-center gap-2">
           <svg>...</svg>
           Select Project
         </span>
       </label>
       <select id="flutterflow-projects-select" class="api-key-input w-full">
         <option value="">Loading projects...</option>
       </select>
       <p class="text-xs text-gray-500 mt-1">
         Select a project to automatically fill Project ID
       </p>
     </div>
   ```
   - Include appropriate icon (reuse from Project ID or new)
   - Initially hidden (`class="hidden"`)

2. Reduce warning boxes size or remove:
   - Orange box (FlutterFlow Direct Commit): Either remove entirely or reduce to smaller text
   - Blue box (Security): Keep but reduce padding/margin to save space
   - Goal: Make room for dropdown without expanding modal height significantly

3. Add "Load Projects" button (optional):
   - Button next to API key input to trigger fetch manually
   - Or automatically trigger on API key validation success
   - Decide: implement automatic fetch on blur if key valid

**Implementation notes:**
- Maintain consistent styling with existing inputs
- Ensure dropdown matches Tailwind classes (`api-key-input`)
- Update modal layout to accommodate new element
  </action>
  <verify>grep -q 'flutterflow-projects-select' index.html</verify>
  <done>Dropdown element exists in modal with proper styling</done>
</task>

<task type="auto">
  <name>Task 3: Connect API key input to project fetching</name>
  <files>app.js</files>
  <action>
1. Add event listener for FlutterFlow API key input:
   - On blur (`blur` event) or after validation (`input` with debounce)
   - When key is valid (passes `validateFlutterFlowApiKey`), call `fetchProjects`

2. Create `fetchProjects` function:
   - Takes API key as parameter
   - Creates temporary FlutterFlowApiClient instance (no projectId needed)
   - Calls `client.listProjects()`
   - Handles loading state (show loading in dropdown)
   - On success: populate dropdown with `<option value="{id}">{name}</option>`
   - On error: show error message below dropdown

3. Connect dropdown selection to Project ID input:
   - When user selects a project from dropdown, set Project ID input value to selected project id
   - Update validation status (green dot)

4. Update validation logic:
   - Project ID can be manually entered OR selected from dropdown
   - Both should trigger same validation

**Implementation notes:**
- Use debounce (300-500ms) to avoid excessive API calls
- Show loading indicator in dropdown
- Handle empty results (no projects)
- Store fetched projects temporarily to avoid refetching on modal reopen
  </action>
  <verify>grep -q 'fetchProjects' app.js</verify>
  <done>API key input triggers project fetch, dropdown populated, selection fills Project ID</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Run `node --check app.js` (syntax check)
- [ ] Load index.html in browser, open API Keys modal
- [ ] Enter valid FlutterFlow API key, see projects load in dropdown
- [ ] Select project, see Project ID auto-filled
- [ ] Warning boxes reduced/removed as planned
- [ ] Save & Close works correctly
</verification>

<success_criteria>

- All tasks completed
- No errors or warnings introduced
- FlutterFlowApiClient.listProjects method works
- Dropdown appears when API key is valid
- Project selection auto-fills Project ID field
- Modal UI remains clean and usable
- No broken existing functionality

</success_criteria>

<output>
After completion, create `.planning/phases/11-project-list-dropdown/11-01-SUMMARY.md`
</output>
---
phase: 04-file-prep
plan: 01
type: execute
wave: 3
depends_on: ["03-03"]
files_modified: [app.js]
autonomous: true
---

<objective>
Create file utilities for custom code extraction and type detection.

Purpose: Detect file types (Action, Widget, Function) and extract code for FlutterFlow commit.
Output: File type detection utilities and code extraction functions.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-api-client/03-03-SUMMARY.md
@app.js
@VS-Code-Extension/src/fileUtils/FileInfo.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create file type detection utilities</name>
  <files>app.js</files>
  <action>Add file type detection utilities after the API client code:

1. Add CodeType enum/constants:
```javascript
/**
 * Code type enumeration for FlutterFlow custom code
 */
const CodeType = {
  ACTION: 'A',
  WIDGET: 'W',
  FUNCTION: 'F',
  DEPENDENCIES: 'D',
  OTHER: 'O',
};
```

2. Add detectCodeType function:
```javascript
/**
 * Detects the type of custom code based on file name and content.
 * @param {string} fileName - Name of the file
 * @param {string} [content] - Optional file content for additional detection
 * @returns {string} Code type (A, W, F, D, or O)
 */
function detectCodeType(fileName, content = '') {
  // Check for dependencies (pubspec.yaml)
  if (fileName === 'pubspec.yaml') {
    return CodeType.DEPENDENCIES;
  }
  
  // Check for Dart files
  if (!fileName.endsWith('.dart') || fileName.endsWith('index.dart')) {
    return CodeType.OTHER;
  }
  
  // Check for custom functions
  if (fileName === 'custom_functions.dart') {
    return CodeType.FUNCTION;
  }
  
  // Detect by content patterns if filename is ambiguous
  if (content) {
    // Check for Action class pattern
    if (content.includes('extends State') || content.includes('StatefulWidget')) {
      // Could be widget or action - check for action patterns
      if (content.includes('Future') && content.includes('BuildContext')) {
        return CodeType.ACTION;
      }
      return CodeType.WIDGET;
    }
    
    // Check for function pattern
    if (content.match(/^\s*(String|int|double|bool|List|Map|dynamic|void)\s+\w+\s*\(/m)) {
      return CodeType.FUNCTION;
    }
  }
  
  // Default to OTHER if can't determine
  return CodeType.OTHER;
}
```

3. Add getFilePathForCodeType function:
```javascript
/**
 * Gets the relative file path based on code type.
 * @param {string} fileName - Original file name
 * @param {string} codeType - Code type (A, W, F, D, O)
 * @returns {string} Relative path in FlutterFlow structure
 */
function getFilePathForCodeType(fileName, codeType) {
  switch (codeType) {
    case CodeType.ACTION:
      return `lib/custom_code/actions/${fileName}`;
    case CodeType.WIDGET:
      return `lib/custom_code/widgets/${fileName}`;
    case CodeType.FUNCTION:
      return 'lib/flutter_flow/custom_functions.dart';
    case CodeType.DEPENDENCIES:
      return 'pubspec.yaml';
    default:
      return fileName;
  }
}
```

4. Add proper JSDoc comments for all functions</action>
  <verify>grep -q 'const CodeType' app.js && grep -q 'function detectCodeType' app.js && grep -q 'function getFilePathForCodeType' app.js</verify>
  <done>File type detection utilities implemented with CodeType constants and detection logic</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] CodeType constants defined
- [ ] detectCodeType function implemented
- [ ] getFilePathForCodeType function implemented
- [ ] Functions handle all code types
</verification>

<success_criteria>

- File type detection utilities created
- CodeType enum defined
- Detection logic handles Actions, Widgets, Functions, and Dependencies
- File path mapping matches FlutterFlow structure
  </success_criteria>

<output>
After completion, create `.planning/phases/04-file-prep/04-01-SUMMARY.md`
</output>

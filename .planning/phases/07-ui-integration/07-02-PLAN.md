---
phase: 07-ui-integration
plan: 02
type: execute
wave: 6
depends_on: ["07-01"]
files_modified: [index.html, app.js]
autonomous: true
---

<objective>
Create commit confirmation modal with code preview.

Purpose: Give users a final review opportunity before committing code to FlutterFlow.
Output: Modal dialog showing code summary and confirmation buttons.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-ui-integration/07-01-SUMMARY.md
@index.html
@app.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create commit confirmation modal HTML</name>
  <files>index.html</files>
  <action>Add a commit confirmation modal to index.html:

1. Add the modal after the existing modals (after walkthrough-modal):
```html
<!-- Commit Confirmation Modal -->
<div id="commit-confirm-modal" class="modal-overlay" onclick="closeCommitConfirmModal(event)">
  <div class="modal-content" onclick="event.stopPropagation()" style="max-width: 640px;">
    <div class="px-6 py-4 border-b border-gray-200 flex items-center justify-between">
      <h2 class="font-semibold text-lg text-gray-900">Confirm Commit to FlutterFlow</h2>
      <button onclick="closeCommitConfirmModal()" class="p-1 hover:bg-gray-100 rounded-lg transition-colors">
        <svg class="w-5 h-5 text-gray-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
        </svg>
      </button>
    </div>

    <div class="p-6 space-y-4 max-h-[60vh] overflow-y-auto">
      <!-- Code Summary -->
      <div>
        <h3 class="font-medium text-gray-900 mb-2">Code Summary</h3>
        <div class="bg-gray-50 rounded-lg p-3 text-sm">
          <div class="grid grid-cols-2 gap-2">
            <div><span class="text-gray-500">File:</span> <span id="confirm-file-name" class="font-medium"></span></div>
            <div><span class="text-gray-500">Type:</span> <span id="confirm-artifact-type" class="font-medium"></span></div>
            <div><span class="text-gray-500">Size:</span> <span id="confirm-file-size" class="font-medium"></span></div>
            <div><span class="text-gray-500">Lines:</span> <span id="confirm-line-count" class="font-medium"></span></div>
          </div>
        </div>
      </div>

      <!-- Dependencies -->
      <div id="confirm-deps-section" class="hidden">
        <h3 class="font-medium text-gray-900 mb-2">Dependencies</h3>
        <div class="bg-blue-50 rounded-lg p-3">
          <p class="text-xs text-blue-700 mb-2">The following packages will be added to pubspec.yaml:</p>
          <ul id="confirm-deps-list" class="text-xs text-blue-800 space-y-1"></ul>
        </div>
      </div>

      <!-- Warnings -->
      <div id="confirm-warnings-section" class="hidden">
        <h3 class="font-medium text-amber-700 mb-2">Warnings</h3>
        <div class="bg-amber-50 rounded-lg p-3">
          <ul id="confirm-warnings-list" class="text-xs text-amber-800 space-y-1"></ul>
        </div>
      </div>

      <!-- Project Info -->
      <div>
        <h3 class="font-medium text-gray-900 mb-2">Target Project</h3>
        <div class="flex items-center gap-2 text-sm">
          <svg class="w-4 h-4 text-orange-500" viewBox="0 0 24 24" fill="currentColor">
            <path d="M12 2L2 7l10 5 10-5-10-5zM2 17l10 5 10-5M2 12l10 5 10-5"/>
          </svg>
          <span id="confirm-project-id" class="text-gray-700"></span>
        </div>
      </div>

      <!-- Code Preview (collapsible) -->
      <div>
        <button onclick="toggleCodePreview()" class="flex items-center gap-2 text-sm text-blue-600 hover:text-blue-700">
          <svg id="code-preview-chevron" class="w-4 h-4 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
          </svg>
          Preview Code
        </button>
        <div id="code-preview-content" class="hidden mt-2">
          <div class="code-container" style="max-height: 300px;">
            <pre id="confirm-code-preview" class="code-font text-gray-300 text-xs"></pre>
          </div>
        </div>
      </div>
    </div>

    <div class="px-6 py-4 border-t border-gray-200 flex gap-3 justify-end">
      <button 
        onclick="closeCommitConfirmModal()" 
        class="px-4 py-2 text-sm font-medium text-gray-600 hover:bg-gray-100 rounded-lg transition-colors"
      >
        Cancel
      </button>
      <button 
        onclick="confirmCommitToFlutterFlow()" 
        class="px-4 py-2 text-sm font-medium text-white bg-orange-500 hover:bg-orange-600 rounded-lg transition-colors"
      >
        Confirm Commit
      </button>
    </div>
  </div>
</div>
```

2. Use the same modal styling as existing modals
3. Make it wider (max-width: 640px) to accommodate code preview</action>
  <verify>grep -q 'commit-confirm-modal' index.html && grep -q 'confirmCommitToFlutterFlow' index.html && grep -q 'toggleCodePreview' index.html</verify>
  <done>Commit confirmation modal HTML added with all sections</done>
</task>

<task type="auto">
  <name>Task 2: Add modal control functions</name>
  <files>app.js</files>
  <action>Add modal control and population functions:

1. Add modal control functions:
```javascript
// Store commit data for confirmation
let pendingCommitData = null;

/**
 * Opens the commit confirmation modal with code details.
 * @param {Object} codeInfo - Prepared code info
 * @param {Object} checks - Pre-commit check results
 * @param {Object} deps - Detected dependencies
 */
function openCommitConfirmModal(codeInfo, checks, deps) {
  pendingCommitData = { codeInfo, checks, deps };
  
  // Populate modal fields
  document.getElementById('confirm-file-name').textContent = codeInfo.fileName;
  document.getElementById('confirm-artifact-type').textContent = codeInfo.artifactType;
  document.getElementById('confirm-file-size').textContent = `${(codeInfo.content.length / 1024).toFixed(1)} KB`;
  document.getElementById('confirm-line-count').textContent = codeInfo.content.split('\n').length;
  
  // Populate project ID
  getApiKey('flutterflow_project_id').then(projectId => {
    document.getElementById('confirm-project-id').textContent = projectId || 'Not configured';
  });
  
  // Populate dependencies
  const depsList = document.getElementById('confirm-deps-list');
  const depsSection = document.getElementById('confirm-deps-section');
  if (deps && Object.keys(deps).length > 0) {
    depsList.innerHTML = Object.entries(deps)
      .map(([name, version]) => `<li>• ${name}: ${version}</li>`)
      .join('');
    depsSection.classList.remove('hidden');
  } else {
    depsSection.classList.add('hidden');
  }
  
  // Populate warnings
  const warningsList = document.getElementById('confirm-warnings-list');
  const warningsSection = document.getElementById('confirm-warnings-section');
  if (checks.warnings && checks.warnings.length > 0) {
    warningsList.innerHTML = checks.warnings.map(w => `<li>• ${w}</li>`).join('');
    warningsSection.classList.remove('hidden');
  } else {
    warningsSection.classList.add('hidden');
  }
  
  // Set code preview
  document.getElementById('confirm-code-preview').textContent = codeInfo.content;
  
  // Reset preview state
  document.getElementById('code-preview-content').classList.add('hidden');
  document.getElementById('code-preview-chevron').style.transform = 'rotate(0deg)';
  
  // Show modal
  const modal = document.getElementById('commit-confirm-modal');
  if (modal) {
    modal.classList.add('open');
  }
}

/**
 * Closes the commit confirmation modal.
 * @param {Event} [event] - Optional click event
 */
function closeCommitConfirmModal(event) {
  if (event && event.target !== event.currentTarget) return;
  const modal = document.getElementById('commit-confirm-modal');
  if (modal) {
    modal.classList.remove('open');
  }
  pendingCommitData = null;
}

/**
 * Toggles the code preview section.
 */
function toggleCodePreview() {
  const content = document.getElementById('code-preview-content');
  const chevron = document.getElementById('code-preview-chevron');
  
  if (content.classList.contains('hidden')) {
    content.classList.remove('hidden');
    chevron.style.transform = 'rotate(90deg)';
  } else {
    content.classList.add('hidden');
    chevron.style.transform = 'rotate(0deg)';
  }
}

/**
 * Confirms the commit after modal review.
 */
async function confirmCommitToFlutterFlow() {
  closeCommitConfirmModal();
  
  if (!pendingCommitData) {
    console.error('No pending commit data');
    return;
  }
  
  const { codeInfo } = pendingCommitData;
  
  // Get pipeline info
  let artifactType = 'CustomWidget';
  let artifactName = 'GeneratedWidget';
  
  if (pipelineState.step1Result) {
    try {
      const spec = JSON.parse(pipelineState.step1Result);
      artifactType = spec.artifactType || 'CustomWidget';
      artifactName = spec.artifactName || 'GeneratedWidget';
    } catch (e) {
      console.warn('Could not parse step 1 result:', e);
    }
  }
  
  // Execute commit
  const result = await executeCommit(codeInfo.content, {
    artifactType,
    artifactName,
    pipelineResult: {
      step1Result: pipelineState.step1Result,
      selectedModel: document.getElementById('code-generator-model')?.value,
    },
  });
  
  // Show result (Phase 8 will enhance this)
  if (result.success) {
    alert(`Success! ${result.message}`);
  } else {
    alert(`Commit failed: ${result.error}`);
  }
  
  pendingCommitData = null;
}
```

2. Update initiateCommitToFlutterFlow to use the modal:
```javascript
// Replace the confirmation logic in initiateCommitToFlutterFlow:
// Instead of: const shouldProceed = await showPreCommitSummary(codeInfo, checks);
// Use: openCommitConfirmModal(codeInfo, checks, extractDependencies(codeInfo.content));
```

3. Add proper JSDoc comments</action>
  <verify>grep -q 'openCommitConfirmModal' app.js && grep -q 'closeCommitConfirmModal' app.js && grep -q 'pendingCommitData' app.js</verify>
  <done>Modal control functions implemented with full population logic</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Commit confirmation modal HTML exists
- [ ] Modal has all sections (summary, deps, warnings, project, preview)
- [ ] Modal control functions work
- [ ] Code preview is toggleable
- [ ] Confirm button triggers commit
</verification>

<success_criteria>

- Commit confirmation modal created
- All sections populated dynamically
- Code preview with toggle
- Proper modal open/close behavior
- Integration with commit flow
  </success_criteria>

<output>
After completion, create `.planning/phases/07-ui-integration/07-02-SUMMARY.md`
</output>

---
phase: 07-ui-integration
plan: 03
type: execute
wave: 6
depends_on: ["07-02"]
files_modified: [index.html, app.js]
autonomous: true
---

<objective>
Integrate commit action with UI feedback during the operation.

Purpose: Provide real-time feedback during the commit process (loading states, progress updates).
Output: UI feedback integration showing commit progress and status updates.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-ui-integration/07-02-SUMMARY.md
@index.html
@app.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add commit progress UI</name>
  <files>index.html</files>
  <action>Add a commit progress overlay to the UI:

1. Add progress overlay HTML after the commit confirmation modal:
```html
<!-- Commit Progress Overlay -->
<div id="commit-progress-overlay" class="modal-overlay" style="background: rgba(0, 0, 0, 0.7);">
  <div class="modal-content" style="max-width: 400px; text-align: center;">
    <div class="p-6">
      <div class="spinner mb-4 mx-auto"></div>
      <h3 class="font-semibold text-gray-900 mb-2" id="progress-title">Committing to FlutterFlow...</h3>
      <p class="text-sm text-gray-500 mb-4" id="progress-message">Preparing code</p>
      
      <!-- Progress Bar -->
      <div class="w-full bg-gray-200 rounded-full h-2 mb-2">
        <div id="commit-progress-bar" class="bg-orange-500 h-2 rounded-full transition-all duration-300" style="width: 0%"></div>
      </div>
      <p class="text-xs text-gray-400" id="progress-detail">Step 1 of 4</p>
    </div>
  </div>
</div>
```

2. Style it to appear centered with a darker overlay
3. Make it non-closable (no close button) since it's a progress indicator</action>
  <verify>grep -q 'commit-progress-overlay' index.html && grep -q 'commit-progress-bar' index.html && grep -q 'progress-title' index.html</verify>
  <done>Commit progress overlay HTML added</done>
</task>

<task type="auto">
  <name>Task 2: Add progress update functions</name>
  <files>app.js</files>
  <action>Add progress UI control functions:

1. Add progress overlay functions:
```javascript
/**
 * Shows the commit progress overlay.
 */
function showCommitProgress() {
  const overlay = document.getElementById('commit-progress-overlay');
  if (overlay) {
    overlay.classList.add('open');
    updateCommitProgress(0, 'Initializing...', 'Step 0 of 4');
  }
}

/**
 * Hides the commit progress overlay.
 */
function hideCommitProgress() {
  const overlay = document.getElementById('commit-progress-overlay');
  if (overlay) {
    overlay.classList.remove('open');
  }
}

/**
 * Updates the commit progress UI.
 * @param {number} percent - Progress percentage (0-100)
 * @param {string} message - Status message
 * @param {string} detail - Detailed step info
 */
function updateCommitProgress(percent, message, detail) {
  const progressBar = document.getElementById('commit-progress-bar');
  const progressMessage = document.getElementById('progress-message');
  const progressDetail = document.getElementById('progress-detail');
  
  if (progressBar) {
    progressBar.style.width = `${percent}%`;
  }
  if (progressMessage) {
    progressMessage.textContent = message;
  }
  if (progressDetail) {
    progressDetail.textContent = detail;
  }
}

/**
 * Maps commit state to progress UI.
 * @param {string} state - CommitState value
 */
function updateProgressFromState(state) {
  const stateProgressMap = {
    [CommitState.IDLE]: { percent: 0, message: 'Ready', detail: '' },
    [CommitState.PREPARING]: { percent: 25, message: 'Preparing code...', detail: 'Step 1 of 4' },
    [CommitState.VALIDATING]: { percent: 50, message: 'Validating...', detail: 'Step 2 of 4' },
    [CommitState.PUSHING]: { percent: 75, message: 'Pushing to FlutterFlow...', detail: 'Step 3 of 4' },
    [CommitState.SUCCESS]: { percent: 100, message: 'Complete!', detail: 'Step 4 of 4' },
    [CommitState.ERROR]: { percent: 100, message: 'Failed', detail: 'Error occurred' },
  };
  
  const progress = stateProgressMap[state];
  if (progress) {
    updateCommitProgress(progress.percent, progress.message, progress.detail);
  }
}
```

2. Listen for commit state changes to update UI:
```javascript
// Add to DOMContentLoaded event listener:
window.addEventListener('commitStateChange', (event) => {
  const { state } = event.detail;
  updateProgressFromState(state);
  
  // Show/hide progress overlay based on state
  if (state === CommitState.PREPARING || 
      state === CommitState.VALIDATING || 
      state === CommitState.PUSHING) {
    showCommitProgress();
  } else if (state === CommitState.SUCCESS || state === CommitState.ERROR) {
    // Keep visible briefly then hide
    setTimeout(hideCommitProgress, 1000);
  }
});
```

3. Update confirmCommitToFlutterFlow to show progress:
```javascript
// At the start of confirmCommitToFlutterFlow:
showCommitProgress();
```

4. Add proper JSDoc comments</action>
  <verify>grep -q 'showCommitProgress' app.js && grep -q 'updateCommitProgress' app.js && grep -q "'commitStateChange'" app.js</verify>
  <done>Progress UI functions implemented with state change listeners</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Progress overlay HTML exists with spinner and progress bar
- [ ] showCommitProgress and hideCommitProgress functions work
- [ ] updateCommitProgress updates UI elements
- [ ] State change listener updates progress automatically
- [ ] Progress shows during commit and hides after completion
</verification>

<success_criteria>

- Commit progress overlay created
- Progress bar with percentage
- Status messages for each step
- State change event integration
- Automatic show/hide based on commit state
  </success_criteria>

<output>
After completion, create `.planning/phases/07-ui-integration/07-03-SUMMARY.md`
</output>

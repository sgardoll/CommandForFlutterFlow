# Phase 12 Plan 03: Single-Artifact Deploy Compatibility

## Goal

Keep the current deploy-to-FlutterFlow flow working through the bundle adapter without implementing full multi-file deployment.

## Requirements

- BUND-02
- BUND-03
- BUND-04
- FIXT-01

## Tasks

1. Add helper accessors for the primary/current artifact in a bundle.
2. Update deploy initiation to prefer bundle artifact metadata when available.
3. Preserve current fallback behavior when bundle metadata is missing.
4. Add tests for primary artifact selection and metadata fallback.

## Verification

- Single-artifact deploy preparation still derives artifact type/name correctly.
- Missing structured metadata falls back to current `CustomWidget` / `GeneratedWidget` behavior.
- `npm test` passes.
- `npm run build` passes.


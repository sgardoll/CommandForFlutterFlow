---
phase: 03-api-client
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: [app.js]
autonomous: true
---

<objective>
Implement pushCode method to send code to FlutterFlow.

Purpose: Enable committing generated custom code directly to the user's FlutterFlow project.
Output: Working pushCode method that uploads custom code to FlutterFlow with proper formatting.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-api-client/03-01-SUMMARY.md
@.planning/phases/03-api-client/03-02-SUMMARY.md
@app.js
@VS-Code-Extension/src/api/FlutterFlowApiClient.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement pushCode method</name>
  <files>app.js</files>
  <action>Add the pushCode method to FlutterFlowApiClient class:

1. First, add the FileWarning type documentation comment:
```javascript
/**
 * @typedef {Object} FileWarning
 * @property {string} fileType - Type of file (action, widget, function, pubspec)
 * @property {string} errorMessage - Error description
 * @property {boolean} isCritical - If true, prevents syncing
 */

/**
 * @typedef {Object} PushCodeResult
 * @property {number} responseCode - HTTP response code
 * @property {string} [errorMessage] - Error message if failed
 * @property {Map<string, FileWarning[]>} [errorMap] - Map of file paths to warnings
 */
```

2. Add the pushCode method:
```javascript
/**
 * Pushes custom code to FlutterFlow.
 * @param {Object} pushCodeRequest - Request object containing code data
 * @param {string} pushCodeRequest.project_id - FlutterFlow project ID
 * @param {string} pushCodeRequest.zipped_custom_code - Base64 encoded zip of custom code
 * @param {string} pushCodeRequest.uid - User identifier
 * @param {string} pushCodeRequest.branch_name - Target branch name
 * @param {string} pushCodeRequest.serialized_yaml - Serialized pubspec.yaml content
 * @param {string} pushCodeRequest.file_map - JSON string of file path to content mapping
 * @param {string} pushCodeRequest.functions_map - JSON string of function definitions
 * @returns {Promise<Response>} Fetch response object
 */
async pushCode(pushCodeRequest) {
  try {
    const response = await fetch(`${this.baseUrl}/syncCustomCodeChanges`, {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'Authorization': `Bearer ${this.apiKey}`,
      },
      body: JSON.stringify(pushCodeRequest),
    });
    
    return response;
  } catch (error) {
    console.error('API Error syncing code:', error);
    throw new Error(`API Error syncing code: ${error.message}`);
  }
}
```

3. Add proper JSDoc for type definitions and method
4. Ensure proper error handling and logging</action>
  <verify>grep -q 'async pushCode(' app.js && grep -q 'syncCustomCodeChanges' app.js</verify>
  <done>pushCode method implemented with proper request structure and error handling</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pushCode method exists in FlutterFlowApiClient
- [ ] Method accepts pushCodeRequest parameter with all required fields
- [ ] Uses correct API endpoint (/syncCustomCodeChanges)
- [ ] Proper authentication with Bearer token
- [ ] Error handling implemented
</verification>

<success_criteria>

- pushCode method implemented
- Proper request structure matching FlutterFlow API
- Error handling with descriptive messages
- Console logging for debugging
- Ready for integration with commit action
  </success_criteria>

<output>
After completion, create `.planning/phases/03-api-client/03-03-SUMMARY.md`
</output>

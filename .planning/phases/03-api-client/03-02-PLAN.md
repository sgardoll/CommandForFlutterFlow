---
phase: 03-api-client
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: [app.js]
autonomous: true
---

<objective>
Implement pullCode method to retrieve code from FlutterFlow.

Purpose: Allow downloading existing custom code from a FlutterFlow project for reference or modification.
Output: Working pullCode method that fetches and extracts code from FlutterFlow.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-api-client/03-01-SUMMARY.md
@app.js
@VS-Code-Extension/src/api/exportCode.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement pullCode method</name>
  <files>app.js</files>
  <action>Add the pullCode method to FlutterFlowApiClient class:

1. Add the method after the constructor/getters:
```javascript
/**
 * Pulls code from FlutterFlow and returns it as a structured object.
 * @returns {Promise<Object>} Object containing file contents mapped by path
 */
async pullCode() {
  console.log(`Pulling code from FlutterFlow project: ${this.projectId}, branch: ${this.branchName || 'main'}`);
  
  try {
    const response = await fetch(`${this.baseUrl}/exportCode`, {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'Authorization': `Bearer ${this.apiKey}`,
      },
      body: JSON.stringify({
        project_id: this.projectId,
        branch_name: this.branchName,
        include_assets: false,
        export_as_module: false,
      }),
    });

    if (!response.ok) {
      const errorText = await response.text();
      throw new Error(`Export failed: ${response.status} - ${errorText}`);
    }

    const data = await response.json();
    
    // The response should contain a download_url or direct content
    // For now, return the parsed response
    return {
      success: true,
      downloadUrl: data.download_url,
      projectId: this.projectId,
      branchName: this.branchName,
    };
  } catch (error) {
    console.error('Error pulling code from FlutterFlow:', error);
    throw error;
  }
}
```

2. Add proper JSDoc comments
3. Handle errors appropriately with try-catch
4. Log operations for debugging

Note: This is a simplified version adapted for browser use. The full implementation would handle ZIP extraction.</action>
  <verify>grep -q 'async pullCode()' app.js && grep -q 'exportCode' app.js</verify>
  <done>pullCode method implemented with proper error handling and logging</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pullCode method exists in FlutterFlowApiClient
- [ ] Method uses correct API endpoint and authentication
- [ ] Error handling is implemented
- [ ] Returns structured response object
</verification>

<success_criteria>

- pullCode method implemented
- Proper fetch API usage with Bearer token
- Error handling with descriptive messages
- Console logging for debugging
  </success_criteria>

<output>
After completion, create `.planning/phases/03-api-client/03-02-SUMMARY.md`
</output>
